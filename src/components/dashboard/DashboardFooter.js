import React, {PropTypes} from 'react';

const DashboardFooter = (props) => {

  return (
    <div>Hello Dashboard Footer</div>
  );
};

DashboardFooter.propTypes = {

  // myProp: PropTypes.string.isRequired
};

export default DashboardFooter;
